# Глава 1: Быстрый старт 🚀

> Всё, что нужно для запуска проекта за 5 минут

---

## 📋 Требования

Перед началом работы убедитесь, что у вас установлены:

- **Node.js** — версия 18.x или выше
- **npm** — версия 9.x или выше
- **Git** — для клонирования репозитория

### Проверка версий

```bash
node --version   # v18.x.x или выше
npm --version    # 9.x.x или выше
git --version    # 2.x.x
```

---

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd sds
```

### 2. Установка зависимостей

```bash
npm install
```

Это установит все необходимые пакеты из `package.json`:

- Vue 3, Vue Router (фреймворк)
- TypeScript (типизация)
- Pinia (state management)
- Axios (HTTP-клиент)
- Element Plus (UI-компоненты)
- Webpack 5 (сборка)
- SCSS (стилизация)

---

## ⚙️ Конфигурация

### Переменные окружения (.env)

Скопируйте `.env.example` в `.env` в корне проекта:

```bash
cp .env.example .env
```

В `.env` укажите:

```bash
# Обязательный параметр
OPENWEATHERMAP_API_KEY=ваш_ключ_с_openweathermap

# Опционально (по умолчанию https://api.openweathermap.org/data/2.5)
OPENWEATHERMAP_BASE=https://api.openweathermap.org/data/2.5
```

Ключ API можно получить на [OpenWeatherMap](https://openweathermap.org/api).

> ⚠️ **Важно**: Файл `.env` добавлен в `.gitignore` и не коммитится в репозиторий!

---

## 🚀 Запуск проекта

### Режим разработки

```bash
npm run dev
```

Проект будет доступен по адресу: `http://localhost:3000`

### Production сборка

```bash
npm run build
```

Собранные файлы появятся в папке `dist/`.

---

## 📁 Структура проекта

```
sds/
├── src/
│   ├── app/              # Инициализация приложения, роутер, DI-провайдеры, PageLoader
│   ├── pages/            # Страницы и роуты (main → WeatherWidget)
│   ├── widgets/          # Виджеты (weather)
│   ├── features/         # Фичи (search-city)
│   ├── entities/         # Сущности (Weather)
│   └── shared/           # config, lib, ui, стили
├── docs/                 # Документация
├── public/               # Статические файлы
└── package.json
```

---

## 🎯 Следующие шаги

После успешного запуска:

1. Откройте `http://localhost:3000` в браузере
2. Изучите [Архитектурные решения](./02-architecture.md)
3. Прочитайте [FSD Принципы](./03-fsd-principles.md)
4. Изучите [DI и Паттерны](./04-di-and-patterns.md)

---

## ❓ Решение проблем

### Порт уже занят

Измените порт в конфигурации Webpack или переменной окружения (если поддерживается).

### Ошибки TypeScript

```bash
npm run type-check
```

### Очистка кеша

```bash
rm -rf node_modules dist
npm install
npm run dev
```

---

**Далее**: [Глава 2: Архитектурные решения](./02-architecture.md) 🏗️
